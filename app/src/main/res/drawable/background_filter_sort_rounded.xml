<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Màu nền cho cụm nút -->
    <solid android:color="?attr/colorSurface" />
    <!-- Hoặc một màu cụ thể nếu bạn muốn, ví dụ: <solid android:color="@color/white_smoke_background"/> -->
    <!-- Bạn có thể tạo màu white_smoke_background trong colors.xml: <color name="white_smoke_background">#F5F5F5</color> -->

    <!-- Độ bo tròn các góc -->
    <corners android:radius="24dp" /> <!-- Bạn có thể điều chỉnh giá trị này theo ý muốn -->

    <!-- (Tùy chọn) Thêm viền nếu muốn -->
    <!--
    <stroke
        android:width="1dp"
        android:color="?attr/colorControlHighlight" />
    -->

    <!-- (Tùy chọn) Thêm padding bên trong shape nếu cần,
         nhưng bạn đã đặt padding trong LinearLayout rồi nên có thể không cần ở đây -->
    <!--
    <padding
        android:left="8dp"
        android:top="4dp"
        android:right="8dp"
        android:bottom="4dp" />
    -->
</shape>